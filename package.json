{
  "name": "super-saturn",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter @super-saturn/web dev",
    "dev:cms": "pnpm --filter @super-saturn/cms develop",
    "build": "pnpm -r run build",
    "build:web": "pnpm --filter @super-saturn/web build"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}